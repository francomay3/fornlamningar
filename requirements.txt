# The pipeline is deliberately near-stdlib: sqlite3, urllib, json, struct and
# zlib cover the crawl, the GeoPackage/WKB parsing and every database stage.
# Only the numeric work needs a dependency.
numpy>=1.25.2

# Not pip-installable, needed by two stages:
#   gdal        (ogr2ogr)   fetch_osm.sh, to extract the OSM layers
#   tippecanoe               build_tiles.py, to write the vector tiles
# On macOS: brew install gdal tippecanoe
